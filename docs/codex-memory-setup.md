# Moved: AI Deployment Guide

This path is kept for older links.

Use [docs/ai-deployment.md](./ai-deployment.md) for the current clean-clone deployment guide.

The old content mixed public deployment steps with memory-design notes and resident-anchor seeding. Current public installs must start from an empty valid data root, avoid guessing user/persona facts, and write resident anchors only after the user explicitly confirms content.
